id = "vcpkg"
display_name = "vcpkg"
lang = "cpp"
metadata_files = ["vcpkg.json"]
lockfiles = ["vcpkg-lock.json"]
metadata_reader = "vcpkg-json"

[commands]
install = "vcpkg install"
build = "cmake --build build"
typecheck = "cmake --build build"
lint = "clang-tidy"
test = "ctest --test-dir build"