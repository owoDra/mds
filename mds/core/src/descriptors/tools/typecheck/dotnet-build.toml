id = "dotnet-build"
match_prefixes = ["dotnet build"]

[behavior]
input = "stdin"
append_file_arg = false

[[behavior.diagnostics]]
pattern = '^(?P<path>.+?)\((?P<line>\d+),(?P<column>\d+)\): (?P<message>.+)$'