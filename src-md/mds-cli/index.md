# mds-cli

## Purpose

Native CLI entrypoint and argument handling for mds.

## Architecture

This package is authored under `src-md/mds-cli/` and synchronized into `.build/rust/mds-cli/` before Cargo commands.

## Exposes

| Kind | Name | Target | Summary |
| --- | --- | --- | --- |
| module | mds-cli | .build/rust/mds-cli | Generated Cargo package. |

## Rules

- Put directory-level design in the nearest `index.md`.
- Keep implementation code in `*.rs.md` files.
- Do not edit generated files under `.build/rust/mds-cli`.
