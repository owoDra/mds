# mds-core

## Purpose

Rust core library for parsing, validating, generating, and initializing mds projects.

## Architecture

This package is authored under `src-md/mds-core/` and synchronized into `.build/rust/mds-core/` before Cargo commands.

## Exposes

| Kind | Name | Target | Summary |
| --- | --- | --- | --- |
| module | mds-core | .build/rust/mds-core | Generated Cargo package. |

## Rules

- Put directory-level design in the nearest `index.md`.
- Keep implementation code in `*.rs.md` files.
- Do not edit generated files under `.build/rust/mds-core`.
