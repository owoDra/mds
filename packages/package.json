{
  "name": "mds-workspace",
  "private": true,
  "description": "Workspace coordination for mds npm distribution packages.",
  "workspaces": [
    "*"
  ],
  "scripts": {
    "check:npm": "npm run check --workspaces --if-present",
    "pack:check": "npm pack --workspaces --dry-run"
  },
  "engines": {
    "node": ">=24",
    "npm": ">=10"
  },
  "license": "MIT"
}
